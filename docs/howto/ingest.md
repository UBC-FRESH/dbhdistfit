# Ingest Module (Draft)

This page will describe how to use the forthcoming `nemora.ingest` pipelines to
transform raw forest inventory releases (e.g., BC FAIB, FIA) into the tidy stand
tables consumed by `nemora.distfit`, `nemora.sampling`, and future modules.

.. todo:: Flesh out ingestion workflows once `DatasetSource` and
          `TransformPipeline` implementations land.
